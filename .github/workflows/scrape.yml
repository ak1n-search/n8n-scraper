# .github/workflows/scrape.yml

name: Scrape Substack with Stealth

on:
  repository_dispatch:
    types: [n8n-trigger]

jobs:
  scrape-and-return:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # --- CHANGE 1: Install the new 'stealth' packages ---
      - name: Install Playwright Stealth library
        run: npm install playwright-extra playwright-extra-plugin-stealth playwright-core

      - name: Install Playwright Browsers
        run: npx playwright install chromium

      - name: Run Playwright and Scrape Data
        id: scrape_step 
        run: |
          scraped_json=$(node -e "
            // --- CHANGE 2: Use 'playwright-extra' and the stealth plugin ---
            const { chromium } = require('playwright-extra');
            const StealthPlugin = require('playwright-extra-plugin-stealth');
            chromium.use(StealthPlugin());

            async function run() {
              const browser = await chromium.launch();
              const page = await browser.newPage();
              await page.goto('https://substack.com/search/banjercito?searching=all_posts', { waitUntil: 'domcontentloaded' });
              
              // Wait for the first post title to appear on the page
              // We'll give it a longer timeout to see if the stealth plugin helps
              await page.waitForSelector('[data-testid=\"post-preview-title\"]', { timeout: 45000 });

              // Now that we know the content is loaded, scrape all the titles
              const titles = await page.locator('[data-testid=\"post-preview-title\"]').allInnerTexts();
              await browser.close();

              return JSON.stringify(titles);
            }
            run().then(console.log);
          ")
          echo "scraped_json=$scraped_json" >> $GITHUB_OUTPUT

      - name: Send data to n8n
        run: |
          curl -X POST -H "Content-Type: application/json" \
          -d "{\"postTitles\": ${{ steps.scrape_step.outputs.scraped_json }} }" \
          "${{ secrets.N8N_WEBHOOK_URL }}"
